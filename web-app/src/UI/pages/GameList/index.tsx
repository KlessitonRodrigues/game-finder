import Header from 'UI/base/Header';
import PageContainer from 'UI/base/PageContainer';
import { Background } from 'UI/base/StyledComponents/background';

const GameListPage = () => {
  return (
    <PageContainer>
      <Header />
      TEST
      <Background />
    </PageContainer>
  );
};

export default GameListPage;
